# gRPC Stub Generation Configuration
# Set to true to force gRPC code generation even during normal builds
# forceGrpcGeneration=false

# IDE Configuration
# This helps IDEs understand when to skip code generation
ide.skip.grpc.generation=true